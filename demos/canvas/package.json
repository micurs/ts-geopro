{
  "name": "canvas",
  "private": true,
  "version": "0.0.4",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite",
    "build": "tsc && vite build",
    "start": "vite preview",
    "preview": "vite preview"
  },
  "dependencies": {
    "@micurs/ts-geopro": "workspace:*"
  }
}
